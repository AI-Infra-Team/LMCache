# Ray worker node join config.
# Usage (on WORKER node):
#   ./start_vllm.py -c configs/ray/worker_redis.yaml
lmcache_config_file: "configs/lmcache/redis.yaml"

ray:
  mode: worker
  # Either set `address: "HEAD_IP:PORT"` or set `head_host` + `port`.
  head_host: "HEAD_IP"
  port: 6387

env: {}
